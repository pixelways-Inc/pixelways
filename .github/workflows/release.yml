name: Deploy to Vercel

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    name: Deploy to Vercel
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Build project
        run: npm run build

      - name: Deploy to Vercel
        run: npx vercel --prod --token --yes 6lPmCaHcgvLrAxtSadKYcNu

        
        env:
          VERCEL_TOKEN: B6lPmCaHcgvLrAxtSadKYcNu

# ⚠️ IMPORTANT: 
# Add your Vercel token as a GitHub secret named VERCEL_TOKEN.
# Never commit tokens directly in workflow files.
